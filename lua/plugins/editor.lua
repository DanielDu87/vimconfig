--==============================================================================
-- Editor 插件配置
--==============================================================================
-- 覆盖 LazyVim 默认编辑器插件设置

return {
	--==============================================================================
	-- which-key.nvim 配置 - 自定义样式
	--==============================================================================
	{
		"folke/which-key.nvim",
		---@diagnostic disable-next-line: missing-fields
		opts = {
			-- 不使用预设，直接配置
			win = {
				width = 0.75,
				height = { min = 4, max = math.huge },
				col = 0.5,
				row = -1,
				border = "rounded",
				-- 设置固定标题
				title = "  ◈ 操作菜单 ◈  ",
				title_pos = "center",
				-- 保持默认 padding
				padding = { 1, 2 },
				-- 窗口选项
				wo = {
					-- 尝试居中对齐
					conceallevel = 0,
				},
			},
		},
	},
}
